# Enable faster command sequences
set -s escape-time 0
set -g repeat-time 300
set -g remain-on-exit off
set -g focus-events on
set -g status-interval 5
setw -g aggressive-resize on
set -g default-command "${SHELL}"
set -g set-clipboard on
set -g history-limit 10000

set-option -g status-position top
set-option -g aggressive-resize off
set-option -g mouse on
set-option -g base-index 1
set-option -g pane-base-index 1

setw -g window-size smallest

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
# set -g @plugin 'egel/tmux-gruvbox'
# set -g @tmux-gruvbox 'dark256' # or 'dark256', 'light', 'light256'

#######################################################################################################################
######################################## TOKYO NIGHT THEME START ######################################################
#######################################################################################################################

# TokyoNight colors for Tmux

set -g mode-style "fg=#7aa2f7,bg=#3b4261"

set -g message-style "fg=#7aa2f7,bg=#3b4261"
set -g message-command-style "fg=#7aa2f7,bg=#3b4261"

set -g pane-border-style "fg=#3b4261"
set -g pane-active-border-style "fg=#7aa2f7"

set -g status "on"
set -g status-justify "left"

set -g status-style "fg=#7aa2f7,bg=#16161e"
# set -g status-style "fg=#7aa2f7,bg=default"

set -g status-left-length "100"
set -g status-right-length "100"

set -g status-left-style NONE
set -g status-right-style NONE

set -g status-left "#[fg=#15161e,bg=#7aa2f7,bold] #S #[fg=#7aa2f7,bg=#16161e,nobold,nounderscore,noitalics]"
set -g status-right "#[fg=#3b4261,bg=#16161e,nobold,nounderscore,noitalics]#[fg=#7aa2f7,bg=#3b4261] %Y-%m-%d  %I:%M %p #[fg=#7aa2f7,bg=#3b4261,nobold,nounderscore,noitalics]#[fg=#15161e,bg=#7aa2f7,bold] #h "
# set -g status-left "#[fg=#15161e,bg=default,bold] #S #[fg=#7aa2f7,bg=default,nobold,nounderscore,noitalics]"
# set -g status-right "#[fg=#3b4261,bg=default,nobold,nounderscore,noitalics]#[fg=#7aa2f7,bg=default] %Y-%m-%d  %I:%M %p #[fg=#7aa2f7,bg=default,nobold,nounderscore,noitalics]#[fg=#15161e,bg=default,bold] #h "

setw -g window-status-activity-style "underscore,fg=#a9b1d6,bg=default"
setw -g window-status-separator ""
setw -g window-status-style "NONE,fg=#a9b1d6,bg=default"
setw -g window-status-format "#[fg=#16161e,bg=default,nobold,nounderscore,noitalics]#[default] #I  #W #F #[fg=#16161e,bg=default,nobold,nounderscore,noitalics]"
setw -g window-status-current-format "#[fg=#16161e,bg=default,nobold,nounderscore,noitalics]#[fg=#7aa2f7,bg=default,bold] #I  #W #F #[fg=#3b4261,bg=default,nobold,nounderscore,noitalics]"

# tmux-plugins/tmux-prefix-highlight support
set -g @prefix_highlight_output_prefix "#[fg=#e0af68]#[bg=default]#[fg=#16161e]#[bg=default]"
set -g @prefix_highlight_output_suffix ""

# set -g @plugin 'seebi/tmux-colors-solarized'
# set -g @colors-solarized '256'

# set -g @plugin 'fabioluciano/tmux-powerkit'
#
# set -g @powerkit_separator_style "none"
# set -g @powerkit_edge_separator_style "none"
# set -g @powerkit_window_index_style "numeric"
#
# # Add spacing between elements
# set -g @powerkit_elements_spacing "both"
# set -g @powerkit_transparent "true"
#
# # Basic configuration
# set -g @powerkit_plugins "ssh,network,git,hostname"
# set -g @powerkit_theme "flexoki"
# set -g @powerkit_theme_variant "dark"
#
# set -g window-style 'bg=default'
# set -g window-active-style 'bg=default'

#######################################################################################################################
######################################## TOKYO NIGHT THEME END ########################################################
#######################################################################################################################

run '~/.tmux/plugins/tpm/tpm'


bind C-h select-pane -L 
bind C-j select-pane -D 
bind C-k select-pane -U 
bind C-l select-pane -R 
bind Up resize-pane -L 
bind Down resize-pane -D 
bind Left resize-pane -U 
bind Right resize-pane -R 
bind \; split-window -h -c "#{pane_current_path}" -p 40
bind \' split-window -v  -c "#{pane_current_path}"
bind C-c new-window -c "#{pane_current_path}"
bind C-z resize-pane -Z
bind-key -T prefix \" command-prompt -T window-target -p index { select-window -t ":%%" }
bind-key C-s set-window-option synchronize-panes\; display-message "synchronize-panes is now #{?pane_synchronized,on,off}"

set-window-option -g mode-keys vi
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"
bind -T copy-mode-vi v send-keys -X begin-selection 
bind-key -n C-S-h swap-window -t -1\; select-window -t -1
bind-key -n C-S-l swap-window -t +1\; select-window -t +1
